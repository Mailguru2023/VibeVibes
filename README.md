# BauMeister - Professional Landing Page

Современный лендинг для строительной компании с Tailwind CSS, темной темой и мультиязычностью.

## 🎯 Особенности

### Технологии
- ✅ **Tailwind CSS** - современный utility-first CSS фреймворк через CDN
- ✅ **Dark Mode** - переключение между светлой и темной темой с сохранением в localStorage
- ✅ **Multi-language (DE/EN)** - полная поддержка немецкого и английского языков
- ✅ **Vanilla JavaScript** - без зависимостей, быстрая загрузка
- ✅ **Cloudflare Pages** - бесплатный хостинг с global CDN
- ✅ **Cloudflare Workers** - serverless обработка форм
- ✅ **Responsive Design** - адаптивная верстка для всех устройств
- ✅ **SEO оптимизация** - структурированные данные, meta-теги
- ✅ **Accessibility** - ARIA атрибуты, keyboard navigation

### Функциональность
- 🌓 **Dark Mode** - переключение темы с иконкой солнца/луны
- 🌍 **Multi-language** - переключение между DE/EN с сохранением выбора
- 🎨 **Parallax эффекты** - фоновые изображения с эффектом параллакса
- 📱 **Мобильное меню** - адаптивная навигация
- 🖼️ **Лайтбокс** - просмотр проектов в полноэкранном режиме
- 🎯 **Фильтрация проектов** - по категориям (Дома, Квартиры, Офисы)
- 📝 **Две формы** - контактная форма + callback модал с таймером
- 🔒 **Защита от спама** - honeypot + rate limiting в serverless функции
- 🎭 **Reveal on scroll** - анимации появления элементов при прокрутке
- ⭐ **FAQ аккордеон** - раскрывающиеся ответы на вопросы
- 🎠 **Testimonials slider** - автоматическая ротация отзывов
- 💬 **Tawk.to интеграция** - онлайн-чат поддержки
- 📞 **WhatsApp/Telegram** - быстрые ссылки на мессенджеры
- ⬆️ **Back to top** - кнопка возврата наверх
- ✅ **Мультиязычная валидация форм** - подсказки на выбранном языке
- 🎯 **Фокус на первом пустом поле** - умная валидация форм

## 📂 Структура проекта

```
.
├── index.html              # Главная страница
├── styles.css              # Все стили (CSS Variables, responsive)
├── app.js                  # JavaScript логика (1025 строк)
├── package.json            # NPM зависимости
├── wrangler.toml           # Конфигурация Cloudflare
├── manifest.json           # PWA манифест
├── sitemap.xml             # Карта сайта для SEO
├── robots.txt              # Правила для поисковых роботов
├── _headers                # HTTP заголовки безопасности
├── _redirects              # Настройка редиректов
├── .gitignore              # Git ignore правила
├── .env.example            # Пример переменных окружения
├── README.md               # Этот файл
├── DEPLOYMENT-CLOUDFLARE.md # Инструкция по развертыванию на Cloudflare
│
├── functions/
│   └── functions/
│       └── lead.js         # Serverless функция обработки заявок
│
├── admin/
│   └── config.yml          # Конфигурация Netlify CMS
│
├── _content/               # Контент для CMS
│   ├── services/           # Услуги
│   ├── projects/           # Портфолио проектов
│   └── testimonials/       # Отзывы клиентов
│
└── public/
    ├── images/             # Статические изображения
    │   ├── favicon.svg
    │   └── ...
    └── uploads/            # Загрузки через CMS
```

## 🚀 Быстрый старт

### Локальная разработка

```bash
# 1. Установить зависимости
npm install

# 2. Запустить dev сервер
npm start

# Откроется на http://localhost:8888
# Netlify Functions будут работать на /.netlify/functions/*
```

### Тестирование форм локально

```bash
# Запустить с переменными окружения
NOTIFY_EMAIL=test@example.com npm start
```

## 📦 Развертывание на Netlify

### Метод 1: Через Git (рекомендуется)

1. **Создайте Git репозиторий:**
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main
```

2. **Подключите к Netlify:**
   - Перейдите на [app.netlify.com](https://app.netlify.com)
   - "Add new site" → "Import an existing project"
   - Выберите ваш репозиторий
   - Build settings оставьте по умолчанию
   - Deploy!

3. **Настройте переменные окружения:**
   - Site settings → Environment variables
   - Добавьте: `NOTIFY_EMAIL`, `SENDGRID_KEY`, `ZAP_WEBHOOK`
   - См. `.env.example` для примера

### Метод 2: Через Netlify CLI

```bash
# Установить CLI
npm install -g netlify-cli

# Авторизоваться
netlify login

# Деплой
netlify deploy --prod
```

## ⚙️ Конфигурация

### Переменные окружения

Создайте в Netlify Dashboard → Site settings → Environment variables:

| Переменная | Обязательно | Описание |
|------------|-------------|----------|
| `NOTIFY_EMAIL` | ✅ | Email для получения заявок |
| `SENDGRID_KEY` | ❌ | API ключ SendGrid |
| `FROM_EMAIL` | ❌ | Email отправителя |
| `ZAP_WEBHOOK` | ❌ | Zapier webhook URL |

### Настройка SendGrid

1. Регистрация: [sendgrid.com](https://sendgrid.com)
2. Создать API Key: Settings → API Keys
3. Добавить в Netlify как `SENDGRID_KEY`

### Настройка Zapier

1. Создать Zap: [zapier.com](https://zapier.com)
2. Триггер: "Webhooks by Zapier" → "Catch Hook"
3. Действие: Google Sheets / CRM / Email
4. Добавить webhook URL в Netlify как `ZAP_WEBHOOK`

## 🎨 Кастомизация

### Темная тема

Сайт автоматически определяет системные настройки пользователя. Пользователи могут переключать тему кнопкой в верхнем баре.

**Настройка цветов темной темы** в `styles.css`:

```css
/* Light Mode */
:root {
  --color-primary: #f9c800;      /* Желтый акцент */
  --color-bg-main: #ffffff;      /* Основной фон */
  --color-text-main: #333333;    /* Основной текст */
}

/* Dark Mode */
.dark {
  --color-bg-main: #111827;      /* Темный фон */
  --color-text-main: #d1d5db;    /* Светлый текст */
}
```

### Мультиязычность (DE/EN)

Все переводы хранятся в `app.js` в объекте `translations`. Язык сохраняется в localStorage.

**Добавление нового языка:**

1. Добавьте новый объект в `translations` (например, `fr` для французского)
2. Добавьте кнопку переключения языка в топ-бар
3. Добавьте обработчик в `initLanguage()`

**Изменение переводов:**

```javascript
const translations = {
  de: {
    hero_title: 'Ваш новый заголовок',
    // ...
  },
  en: {
    hero_title: 'Your new title',
    // ...
  }
};
```

### Изменение контента

**Услуги, портфолио, FAQ, отзывы** редактируются в массивах данных в `app.js`:

```javascript
const services = [ /* ... */ ];
const portfolioData = [ /* ... */ ];
const faqData = [ /* ... */ ];
const testimonialsData = [ /* ... */ ];
```

### Добавление изображений

1. Оптимизируйте изображения (WebP, сжатие)
2. Используйте Unsplash API или загрузите в `/public/images/`
3. Используйте относительные пути: `/public/images/your-image.jpg`

## 🔒 Безопасность

### Реализованные меры:

- ✅ Content Security Policy
- ✅ X-Frame-Options: DENY
- ✅ Rate limiting (5 req/min)
- ✅ Honeypot защита
- ✅ Input validation (client + server)
- ✅ HTTPS по умолчанию (Netlify)
- ✅ Sanitization пользовательского ввода

## 📊 SEO оптимизация

### Включено:

- ✅ Semantic HTML5
- ✅ Meta теги (title, description, OG)
- ✅ JSON-LD structured data (LocalBusiness)
- ✅ Sitemap.xml
- ✅ Robots.txt
- ✅ Fast loading (<1s)
- ✅ Mobile-friendly
- ✅ Accessibility (WCAG 2.1)

### Проверить SEO:

```bash
# Lighthouse
npm install -g lighthouse
lighthouse https://yourdomain.com --view

# Или используйте онлайн инструменты:
# - PageSpeed Insights: https://pagespeed.web.dev/
# - GTmetrix: https://gtmetrix.com/
```

## 🧪 Тестирование

### Чек-лист перед запуском:

- [ ] Формы работают (отправка + получение на email)
- [ ] Все ссылки корректны
- [ ] Мобильная версия выглядит хорошо
- [ ] Изображения загружаются
- [ ] Анимации работают (и отключаются при prefers-reduced-motion)
- [ ] Контактные данные актуальны
- [ ] Cookie banner работает
- [ ] Модальное окно открывается/закрывается
- [ ] Фильтры портфолио работают
- [ ] Лайтбокс открывается при клике на изображения
- [ ] Google Analytics трекает (если настроен)

### Инструменты тестирования:

```bash
# Проверка доступности
npm install -g pa11y
pa11y https://yourdomain.com

# Проверка производительности
lighthouse https://yourdomain.com --only-categories=performance

# Проверка мобильной версии
# Используйте Chrome DevTools (F12) → Toggle device toolbar
```

## 📱 Мобильная версия

Полностью адаптивная верстка:

- 📱 **Mobile** (< 768px): Одноколоночный layout, мобильное меню
- 💻 **Tablet** (768px - 1024px): Двухколоночный layout
- 🖥️ **Desktop** (> 1024px): Полная версия с навигацией

## 🌐 Поддержка браузеров

- ✅ Chrome (последние 2 версии)
- ✅ Firefox (последние 2 версии)
- ✅ Safari (последние 2 версии)
- ✅ Edge (последние 2 версии)
- ⚠️ IE11 (частичная поддержка, рекомендуется обновление)

## 📝 Netlify CMS

### Включение CMS:

1. Site settings → Identity → Enable Identity
2. Settings → Identity → Enable Git Gateway
3. Identity → Invite users
4. Откройте `/admin` для доступа

### Управление контентом:

- **Services:** Добавление/редактирование услуг
- **Projects:** Управление портфолио
- **Testimonials:** Отзывы клиентов

## 🆘 Troubleshooting

### Формы не работают

```bash
# Проверьте логи функций
netlify functions:log lead

# Проверьте переменные окружения
netlify env:list
```

### CSS не применяется

- Очистите кеш браузера (Ctrl+Shift+R)
- Проверьте путь к файлу в `index.html`
- Проверьте консоль браузера (F12) на ошибки

### Анимации не работают

- Проверьте, не включен ли `prefers-reduced-motion`
- Откройте консоль: должно быть сообщение "BauMeister Website Loaded"
- Проверьте ошибки JavaScript в консоли

## 📚 Дополнительные ресурсы

- [Netlify Documentation](https://docs.netlify.com/)
- [Netlify Functions Guide](https://docs.netlify.com/functions/overview/)
- [Netlify CMS Docs](https://www.netlifycms.org/docs/)
- [Web Accessibility Guide](https://www.w3.org/WAI/WCAG21/quickref/)

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте [DEPLOYMENT.md](./DEPLOYMENT.md)
2. Проверьте логи в Netlify Dashboard
3. Откройте консоль браузера (F12)
4. Свяжитесь с техподдержкой

## 📄 Лицензия

MIT License - свободное использование для коммерческих и личных проектов.

---

## ✨ Быстрые команды

```bash
npm start          # Запуск dev сервера
npm run deploy     # Production deploy
npm test           # Запуск тестов (если настроены)
```

---

**Создано с ❤️ для BauMeister**

Версия: 1.0.0 | Дата: Декабрь 2025