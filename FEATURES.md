# BauMeister v2.0 - Новые возможности

## 🌓 Темная тема (Dark Mode)

### Как работает
- Автоматически определяет системные настройки пользователя
- Кнопка переключения в правом верхнем углу (иконка солнца/луны)
- Выбор сохраняется в `localStorage` и применяется при следующем визите

### Технические детали
```javascript
// Переключение темы
localStorage.theme = 'dark' // или 'light'
document.documentElement.classList.toggle('dark')
```

### Кастомизация цветов
Отредактируйте CSS переменные в `styles.css`:

```css
/* Светлая тема */
:root {
  --color-bg-main: #ffffff;
  --color-text-main: #333333;
}

/* Темная тема */
.dark {
  --color-bg-main: #111827;
  --color-text-main: #d1d5db;
}
```

---

## 🌍 Мультиязычность (DE/EN)

### Как работает
- Язык по умолчанию: Немецкий (DE)
- Переключатель в верхнем баре: DE / EN
- Выбор сохраняется в `localStorage`
- Весь контент переводится динамически без перезагрузки

### Технические детали
Все переводы хранятся в `app.js`:

```javascript
const translations = {
  de: {
    hero_title: 'Wir gestalten Ihre Räume neu',
    // ...
  },
  en: {
    hero_title: 'We Transform Your Spaces',
    // ...
  }
};
```

### Добавление нового языка

1. **Добавьте переводы в app.js:**
```javascript
const translations = {
  de: { /* ... */ },
  en: { /* ... */ },
  fr: { // Новый язык
    hero_title: 'Nous transformons vos espaces',
    // ... все остальные ключи
  }
};
```

2. **Добавьте кнопку в HTML:**
```html
<div class="lang-switcher">
  <button id="lang-de">DE</button>
  <span>/</span>
  <button id="lang-en">EN</button>
  <span>/</span>
  <button id="lang-fr">FR</button> <!-- Новая кнопка -->
</div>
```

3. **Добавьте обработчик в app.js:**
```javascript
document.getElementById('lang-fr').addEventListener('click', () => switchLanguage('fr'));
```

### Как добавить переводимый текст

1. **В HTML добавьте атрибут:**
```html
<h1 data-translate="my_new_text">Default text</h1>
```

2. **В app.js добавьте переводы:**
```javascript
const translations = {
  de: {
    my_new_text: 'Mein neuer Text',
    // ...
  },
  en: {
    my_new_text: 'My new text',
    // ...
  }
};
```

---

## 🎨 Компоненты

### 1. Services (Услуги)
**Расположение:** Секция `#services`
**Данные:** `services` массив в `app.js`

Добавить новую услугу:
```javascript
const services = [
  // ... существующие
  {
    icon: '<svg>...</svg>', // SVG иконка
    title_de: 'Neue Dienstleistung',
    title_en: 'New Service',
    desc_de: 'Beschreibung auf Deutsch',
    desc_en: 'Description in English'
  }
];
```

### 2. Portfolio (Портфолио)
**Расположение:** Секция `#portfolio`
**Данные:** `portfolioData` массив в `app.js`
**Фильтры:** all, houses, apartments, offices

Добавить новый проект:
```javascript
const portfolioData = [
  // ... существующие
  {
    image: 'https://example.com/image.jpg',
    title_de: 'Projektname',
    title_en: 'Project Name',
    desc_de: 'Kurze Beschreibung',
    desc_en: 'Short description',
    category: 'houses' // или 'apartments', 'offices'
  }
];
```

### 3. FAQ (Вопросы-ответы)
**Расположение:** Секция `#about`
**Данные:** `faqData` массив в `app.js`

Добавить новый вопрос:
```javascript
const faqData = [
  // ... существующие
  {
    question_de: 'Ihre Frage?',
    question_en: 'Your question?',
    answer_de: 'Antwort auf Deutsch',
    answer_en: 'Answer in English'
  }
];
```

### 4. Testimonials (Отзывы)
**Расположение:** Секция `#testimonials`
**Данные:** `testimonialsData` массив в `app.js`
**Автоматическая ротация:** Каждые 5 секунд

Добавить новый отзыв:
```javascript
const testimonialsData = [
  // ... существующие
  {
    text_de: 'Text auf Deutsch',
    text_en: 'Text in English',
    author: 'Name',
    location_de: 'Stadt',
    location_en: 'City',
    rating: 5
  }
];
```

---

## 🎭 Анимации

### Reveal on Scroll
Элементы с классом `.reveal` анимированно появляются при скролле.

**Добавить анимацию:**
```html
<div class="reveal">
  Этот контент появится при скролле
</div>
```

**Настроить задержку:**
```html
<div class="reveal" style="transition-delay: 150ms;">
  Появится через 150ms
</div>
```

### Parallax
Фоновые изображения с эффектом параллакса.

**Классы:**
- `.parallax-bg` - основной класс
- `.hero-bg` - фон для hero секции
- `.cta-bg` - фон для CTA секции

**Изменить изображение:**
```css
.hero-bg {
  background-image: url('your-image-url.jpg');
}
```

---

## 📱 Модальные окна

### 1. Lightbox (Просмотр изображений)
Автоматически открывается при клике на проект в портфолио.

**Программное открытие:**
```javascript
openLightbox(0); // Открыть первый проект
```

### 2. Callback Modal (Заказ звонка)
Модальное окно с формой обратного звонка и таймером (10 минут).

**Триггеры:**
- Кнопка "Rückruf anfordern" в хедере
- Кнопка в мобильном меню

**Функции:**
```javascript
openCallbackModal();  // Открыть
closeCallbackModal(); // Закрыть
startCallbackTimer(); // Запустить таймер
```

---

## 📝 Формы

### Контактная форма
**Расположение:** Секция `#contact`
**Endpoint:** `/.netlify/functions/lead`

### Callback форма
**Расположение:** Callback Modal
**Endpoint:** `/.netlify/functions/lead`

### Защита от спама
- ✅ Honeypot поле (скрытое `name="website"`)
- ✅ Rate limiting на сервере (5 req/min)
- ✅ Валидация на клиенте и сервере

---

## 🎯 Полезные функции

### Smooth Scroll
Все внутренние ссылки имеют плавную прокрутку.

### Active Navigation
Активная навигационная ссылка подсвечивается при скролле к соответствующей секции.

### Back to Top
Кнопка возврата наверх появляется после прокрутки 500px.

### Mobile Menu
Адаптивное меню для мобильных устройств с плавной анимацией.

---

## 🔧 Настройка

### Изменить акцентный цвет
```css
:root {
  --color-primary: #f9c800; /* Желтый по умолчанию */
}
```

### Изменить шрифт
```html
<!-- В head секции index.html -->
<link href="https://fonts.googleapis.com/css2?family=YourFont:wght@400;700&display=swap" rel="stylesheet">
```

```css
/* В styles.css */
:root {
  --font-family: 'YourFont', sans-serif;
}
```

### Изменить контактные данные
Отредактируйте в `index.html`:
- Телефон: `+43 664 1234567`
- Email: `info@baumeister.at`
- Адрес: `Musterstraße 1, 1010 Wien`

### Настроить Tawk.to (онлайн-чат)
Замените ID в `index.html`:
```javascript
s1.src='https://embed.tawk.to/YOUR_PROPERTY_ID/YOUR_WIDGET_ID';
```

---

## 📊 Аналитика

### Google Analytics (готово к интеграции)
Добавьте код отслеживания в `<head>` секцию `index.html`.

### Отслеживание конверсий
Формы отправляют события, которые можно отслеживать:
- Contact form submission
- Callback request

---

## 🚀 Производительность

### Оптимизации
- ✅ Lazy loading изображений (`loading="lazy"`)
- ✅ Preconnect для внешних ресурсов
- ✅ Intersection Observer для анимаций
- ✅ CSS Variables вместо inline styles
- ✅ Debounced scroll events
- ✅ Tailwind CDN (production должен использовать build)

### Рекомендации для production
1. Использовать собственный build Tailwind вместо CDN
2. Минифицировать CSS и JS
3. Оптимизировать изображения (WebP, сжатие)
4. Настроить CDN для статических файлов
5. Включить HTTP/2

---

## 🛠️ Отладка

### Проверить текущий язык
```javascript
console.log(currentLang); // 'de' или 'en'
```

### Проверить текущую тему
```javascript
console.log(localStorage.theme); // 'dark' или 'light'
```

### Проверить сохраненные данные
```javascript
console.log(localStorage); // Все сохраненные данные
```

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что все файлы загружены корректно
3. Проверьте, что Netlify Functions настроены правильно
4. Обратитесь к README.md для подробных инструкций

---

**Версия:** 2.0.0  
**Дата обновления:** 3 декабря 2024
